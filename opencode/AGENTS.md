This opencode config is shared across an Ubuntu devbox and a macOS laptop checkout.
Detect the current host before assuming paths, service managers, or OS-specific tooling. On the Ubuntu devbox, prefer local commands and paths from the devbox as the source of truth when diagnosing or changing config.
